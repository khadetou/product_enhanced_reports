<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        FIXES for sale report:
        1. Removed 'd-flex flex-nowrap' from combo lines - caused PDF rendering issues
        2. Added proper colspan handling for combo lines
        3. Added HTML sanitization via get_description_sale_for_pdf() method
        4. Preserve original line.name when no product description exists
        5. Avoid duplicating content that's already in line.name
    -->
    <template id="report_saleorder_document_enhanced" inherit_id="sale.report_saleorder_document">

        <!-- Replace the product name display in normal product lines -->
        <xpath expr="//tr[@name='tr_product']/td[@name='td_product_name']" position="replace">
            <td name="td_product_name" t-att-class="padding_class">
                <!-- Display product name in bold -->
                <strong t-if="line.product_id">
                    <span t-field="line.product_id.display_name">Product Name</span>
                </strong>
                <strong t-else="">
                    <t t-set="name_parts" t-value="(line.name or '').split('\n', 1)"/>
                    <span t-out="name_parts[0] if name_parts else ''">Product Name</span>
                </strong>
                <!-- Display description below the product name -->
                <t t-if="line.product_id and line.product_id.description_sale">
                    <!-- Use t-out with sanitized HTML for PDF compatibility -->
                    <div t-out="line.product_id.get_description_sale_for_pdf()"
                          class="text-muted small mt-1"
                          style="font-size: 0.9em; color: #666;"/>
                </t>
                <!-- Show line.name description when no product_id or no description_sale -->
                <t t-elif="not line.product_id or (line.name and line.product_id and not line.product_id.description_sale)">
                    <t t-set="name_parts" t-value="(line.name or '').split('\n', 1)"/>
                    <t t-if="len(name_parts) > 1 and name_parts[1]">
                        <br/>
                        <span t-out="name_parts[1]" class="text-muted small">Description</span>
                    </t>
                </t>
            </td>
        </xpath>

        <!-- Also update combo lines to have bold product names -->
        <!-- FIXED: Removed d-flex/flex-nowrap that breaks PDF table rendering -->
        <!-- FIXED: Preserved dynamic colspan from original template -->
        <xpath expr="//tr[@name='tr_combo']/td[@name='td_combo_name']" position="replace">
            <td name="td_combo_name"
                t-att-colspan="3 + (1 if display_discount else 0) + (1 if display_taxes else 0)"
                t-att-class="padding_class"
                style="vertical-align: top;">
                <span class="me-1" style="width: 20px; display: inline-block;">●</span>
                <strong>
                    <span t-field="line.product_id.with_context(display_default_code=False).display_name">
                        Burger Patty
                    </span>
                </strong>
                <t t-if="line.product_id.description_sale">
                    <div t-out="line.product_id.get_description_sale_for_pdf()"
                          class="text-muted small mt-1"
                          style="font-size: 0.9em; color: #666; margin-left: 28px;"/>
                </t>
            </td>
        </xpath>

    </template>

</odoo>
